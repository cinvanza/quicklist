<div class="sign-up-container mt-3 mb-3">
  <h1><strong>Get Started</strong></h1>

  <p>Already have an account? <%= link_to "Sign in", new_user_session_path %></p>

  <%= simple_form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
    <%= f.error_notification %>

    <div class="form-inputs">
      <%= f.input :first_name,
                  required: true,
                  autofocus: true,
                  input_html: { autocomplete: "first_name" }%>
      <%= f.input :last_name,
                  required: true,
                  input_html: { autocomplete: "last_name" }%>
      <%= f.input :country,
                  collection: [ "Argentina", "United States", "Canada", "Mexico", "United Kingdom", "Germany", "France"],
                  required: true,
                  input_html: { autocomplete: "country" }%>
      <%= f.input :currency,
                  collection: ["USD", "CAD", "MXN", "GBP", "EUR", "JPY"],
                  required: true,
                  input_html: { autocomplete: "currency" }%>
      <%= f.input :email,
                  required: true,
                  input_html: { autocomplete: "email" }%>
      <%= f.input :password,
                  required: true,
                  hint: ("#{@minimum_password_length} characters minimum" if @minimum_password_length),
                  input_html: { autocomplete: "new-password" } %>
      <%= f.input :password_confirmation,
                  required: true,
                  input_html: { autocomplete: "new-password" } %>
      <%= f.input :photo, as: :file, required: false %>
    </div>

    <div class="form-actions">
      <%= f.button :submit, "Sign up", class: "signup-btn" %>
    </div>
    <% end %>

  <%= render "devise/shared/links" %>
</div>
