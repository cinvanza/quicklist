<h1>Edit Product</h1>

<%= simple_form_for [@list, @product] do |f| %>
  <%= f.input :name %>
  <%= f.input :price %>

  <%= f.button :submit, "Update Product" %>
<% end %>
<%= link_to 'Back', list_path(@list) %>
